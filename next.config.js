/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'syoveuubcihjabyptwsz.supabase.co'
      }
    ]
  }
};

module.exports = nextConfig;
